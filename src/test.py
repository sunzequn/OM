import networkx as nx
import numpy as np

# graph = nx.Graph()
# graph.add_edge(1, 2, weight=4)
# graph.add_edge(1, 4, weight=3)
# graph.add_edge(2, 3, weight=3)
# print(nx.max_weight_matching(graph, maxcardinality=True))

# print(np.log2(1.0001))

print(1 < 0.4)
